name: GitHub Streak Keeper

on:
  schedule:
    - cron: '0 5 * * *'  # Every day at 5 AM UTC (10:30 AM IST)
  workflow_dispatch:

jobs:
  streak:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Make a dummy update to keep streak
        run: |
          echo "$(date) - 🌱 Keep the streak alive!" >> streak.txt

      - name: Commit and push changes
        run: |
          git config --global user.name "Paawan Bot"
          git config --global user.email "bot@users.noreply.github.com"
          git add streak.txt
          git commit -m "🟩 Automated commit to keep streak"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/PaawanSofTech/ADF.git

